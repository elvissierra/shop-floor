#DATABASE_URL=postgresql+psycopg://<USERNAME>:<PASSWORD>@<HOST>:<PORT>/<DATABASE>?<OPTIONS>

# --------------------------
# Postgres connection (containers talk over the Docker network; NEVER use localhost here)
# --------------------------
DB_USER=sf_user
DB_PASSWORD=shopfloor_pass
DB_NAME=shopfloor_db
DB_HOST=db
DB_PORT=5432

# SQLAlchemy URL used by the backend app (keep both names for compatibility)
DATABASE_URL=postgresql+psycopg://sf_user:shopfloor_pass@db:5432/shopfloor_db

# --------------------------
# App / API config
# --------------------------
TRUSTED_HOSTS=["localhost","127.0.0.1"]
BACKEND_CORS_ORIGINS=["http://localhost","http://127.0.0.1","http://localhost:5173","http://127.0.0.1:5173"]